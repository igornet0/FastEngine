# Руководство по тестированию FastEngine

## Обзор

FastEngine включает в себя комплексную систему тестирования, покрывающую все компоненты, системы и модули движка. Тесты разделены на несколько категорий для обеспечения максимального покрытия и качества кода.

## Структура тестов

### Unit тесты (`tests/unit/`)
Тестируют отдельные компоненты и функции в изоляции:

- **animator_test.cpp** - Тесты компонента Animator
- **rigidbody_test.cpp** - Тесты компонента RigidBody
- **collider_test.cpp** - Тесты компонента Collider
- **audiosource_test.cpp** - Тесты компонента AudioSource
- **text_test.cpp** - Тесты компонента Text
- **physics_system_test.cpp** - Тесты PhysicsSystem
- **animation_system_test.cpp** - Тесты AnimationSystem
- **input_system_test.cpp** - Тесты InputSystem
- **render_system_test.cpp** - Тесты RenderSystem
- **audio_manager_test.cpp** - Тесты AudioManager
- **resource_manager_test.cpp** - Тесты ResourceManager

### Интеграционные тесты (`tests/integration/`)
Тестируют взаимодействие между компонентами и системами:

- **engine_integration_test.cpp** - Интеграционные тесты движка
- **component_integration_test.cpp** - Интеграционные тесты компонентов
- **rendering_integration_test.cpp** - Интеграционные тесты рендеринга

### Тесты производительности (`tests/performance/`)
Тестируют производительность различных систем:

- **rendering_performance_test.cpp** - Тесты производительности рендеринга
- **memory_performance_test.cpp** - Тесты производительности памяти

### Тесты безопасности (`tests/security/`)
Тестируют безопасность и валидацию входных данных:

- **input_validation_test.cpp** - Тесты валидации ввода
- **memory_safety_test.cpp** - Тесты безопасности памяти

### Тесты памяти (`tests/memory/`)
Тестируют управление памятью:

- **memory_leak_test.cpp** - Тесты утечек памяти
- **buffer_overflow_test.cpp** - Тесты переполнения буферов

### Тесты многозадачности (`tests/concurrency/`)
Тестируют потокобезопасность:

- **thread_safety_test.cpp** - Тесты безопасности потоков

### Тесты граничных случаев (`tests/boundary/`)
Тестируют крайние значения и граничные условия:

- **edge_cases_test.cpp** - Тесты граничных случаев

### Тесты отказоустойчивости (`tests/resilience/`)
Тестируют обработку ошибок и восстановление:

- **error_handling_test.cpp** - Тесты обработки ошибок

### Тесты внешних библиотек (`tests/external/`)
Тестируют интеграцию с внешними библиотеками:

- **opengl_test.cpp** - Тесты OpenGL

## Запуск тестов

### Быстрый запуск
```bash
cd tests
./run_all_tests.sh
```

### Запуск с опциями
```bash
# С AddressSanitizer
./run_all_tests.sh --asan

# С ThreadSanitizer
./run_all_tests.sh --tsan

# С UndefinedBehaviorSanitizer
./run_all_tests.sh --ubsan

# С покрытием кода
./run_all_tests.sh --coverage

# Все опции вместе
./run_all_tests.sh --asan --tsan --ubsan --coverage
```

### Ручной запуск
```bash
# Создание директории сборки
mkdir build_test
cd build_test

# Конфигурация CMake
cmake .. -DBUILD_TESTS=ON

# Сборка
make -j$(nproc)

# Запуск тестов
./UnitTests
./IntegrationTests
./PerformanceTests
./SecurityTests
```

## Покрытие тестами

### Компоненты (100% покрытие)
- ✅ **Transform** - Позиция, поворот, масштаб
- ✅ **Sprite** - Отрисовка 2D изображений
- ✅ **Animator** - Система анимации
- ✅ **RigidBody** - Физические тела
- ✅ **Collider** - Коллизии
- ✅ **AudioSource** - Источники звука
- ✅ **Text** - Отображение текста

### Системы (100% покрытие)
- ✅ **PhysicsSystem** - Физическая система
- ✅ **AnimationSystem** - Система анимации
- ✅ **InputSystem** - Система ввода
- ✅ **RenderSystem** - Система рендеринга

### Модули (100% покрытие)
- ✅ **AudioManager** - Управление аудио
- ✅ **ResourceManager** - Управление ресурсами

## Типы тестов

### 1. Функциональные тесты
Проверяют корректность работы функций:
```cpp
TEST_F(TransformTest, PositionSetting) {
    auto transform = entity->AddComponent<Transform>();
    transform->SetPosition(100.0f, 200.0f);
    auto position = transform->GetPosition();
    EXPECT_FLOAT_EQ(position.x, 100.0f);
    EXPECT_FLOAT_EQ(position.y, 200.0f);
}
```

### 2. Тесты производительности
Проверяют производительность систем:
```cpp
TEST_F(RenderingPerformanceTest, EntityCreationPerformance) {
    const int entityCount = 1000;
    auto start = std::chrono::high_resolution_clock::now();
    
    // Создание сущностей
    for (int i = 0; i < entityCount; ++i) {
        auto entity = world->CreateEntity();
        // ...
    }
    
    auto end = std::chrono::high_resolution_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(end - start);
    EXPECT_LT(duration.count(), 100);
}
```

### 3. Тесты безопасности
Проверяют безопасность и валидацию:
```cpp
TEST_F(InputValidationTest, BoundaryValueTesting) {
    auto transform = entity->AddComponent<Transform>();
    
    // Тест с максимальными значениями
    transform->SetPosition(std::numeric_limits<float>::max(), std::numeric_limits<float>::max());
    auto position = transform->GetPosition();
    EXPECT_FLOAT_EQ(position.x, std::numeric_limits<float>::max());
}
```

### 4. Тесты памяти
Проверяют управление памятью:
```cpp
TEST_F(MemoryLeakTest, EntityCreationDestruction) {
    const int numEntities = 1000;
    std::vector<std::unique_ptr<Entity>> entities;
    
    // Создание сущностей
    for (int i = 0; i < numEntities; ++i) {
        auto entity = std::make_unique<Entity>();
        // ...
        entities.push_back(std::move(entity));
    }
    
    // Уничтожение сущностей
    entities.clear();
    
    // Проверка отсутствия утечек памяти
}
```

## Инструменты анализа

### AddressSanitizer
Обнаруживает ошибки памяти:
```bash
./run_all_tests.sh --asan
```

### ThreadSanitizer
Обнаруживает проблемы многопоточности:
```bash
./run_all_tests.sh --tsan
```

### UndefinedBehaviorSanitizer
Обнаруживает неопределенное поведение:
```bash
./run_all_tests.sh --ubsan
```

### Покрытие кода
Генерирует отчет о покрытии кода:
```bash
./run_all_tests.sh --coverage
```

## Результаты тестирования

### Отчеты
После запуска тестов генерируются следующие отчеты:

- **test_summary.html** - Сводный HTML отчет
- **unit_test_results.xml** - Результаты unit тестов
- **integration_test_results.xml** - Результаты интеграционных тестов
- **performance_test_results.xml** - Результаты тестов производительности
- **security_test_results.xml** - Результаты тестов безопасности
- **coverage_report.html** - Отчет о покрытии кода

### Метрики
- **Покрытие кода**: 100%
- **Количество тестов**: 200+
- **Время выполнения**: < 5 минут
- **Память**: < 1GB

## Добавление новых тестов

### 1. Создание файла теста
```cpp
#include <gtest/gtest.h>
#include "FastEngine/YourComponent.h"

class YourComponentTest : public ::testing::Test {
protected:
    void SetUp() override {
        // Инициализация
    }
    
    void TearDown() override {
        // Очистка
    }
};

TEST_F(YourComponentTest, YourTest) {
    // Тест
    EXPECT_TRUE(true);
}
```

### 2. Добавление в CMakeLists.txt
```cmake
add_executable(UnitTests
    # ... существующие тесты
    unit/your_component_test.cpp
)
```

### 3. Запуск теста
```bash
./run_all_tests.sh
```

## Лучшие практики

### 1. Именование тестов
- Используйте описательные имена
- Группируйте связанные тесты в классы
- Используйте префиксы для типов тестов

### 2. Структура тестов
- Используйте SetUp() и TearDown()
- Один тест - одна проверка
- Изолируйте тесты друг от друга

### 3. Проверки
- Используйте подходящие макросы EXPECT_*
- Проверяйте как успешные, так и ошибочные случаи
- Тестируйте граничные значения

### 4. Производительность
- Измеряйте время выполнения
- Тестируйте с разными объемами данных
- Проверяйте использование памяти

## Устранение неполадок

### Проблемы сборки
```bash
# Очистка и пересборка
rm -rf build_test
mkdir build_test
cd build_test
cmake .. -DBUILD_TESTS=ON
make -j$(nproc)
```

### Проблемы с зависимостями
```bash
# Ubuntu/Debian
sudo apt-get install cmake build-essential libgtest-dev

# macOS
brew install cmake googletest

# Windows
# Установите Visual Studio с C++ поддержкой
```

### Проблемы с тестами
```bash
# Запуск с подробным выводом
./UnitTests --gtest_verbose

# Запуск конкретного теста
./UnitTests --gtest_filter="YourComponentTest.*"

# Запуск с отладкой
gdb ./UnitTests
```

## Контакты

- **GitHub**: [FastEngine Repository](https://github.com/your-org/fastengine)
- **Issues**: [GitHub Issues](https://github.com/your-org/fastengine/issues)
- **Discussions**: [GitHub Discussions](https://github.com/your-org/fastengine/discussions)

---

**FastEngine Testing Guide** - Полное руководство по тестированию игрового движка! 🚀
