# Project Simulator для FastEngine

Симулятор проектов для тестирования игр и приложений на движке FastEngine на iOS и Android платформах.

## Возможности

- 🎮 **Управление проектами** - создание, загрузка, сохранение проектов
- 🚀 **Симуляция** - запуск проектов в изолированной среде
- 🧪 **Тестирование** - автоматическое тестирование на разных платформах
- 📱 **Кроссплатформенность** - поддержка iOS, Android и Desktop
- 🎨 **UI система** - интуитивный интерфейс для управления
- 📊 **Профилирование** - мониторинг производительности

## Структура проекта

```
simulator/
├── src/                    # Исходный код
│   ├── core/              # Основные компоненты
│   ├── ui/                # Пользовательский интерфейс
│   ├── projects/          # Управление проектами
│   └── platform/          # Платформенная интеграция
├── include/               # Заголовочные файлы
├── projects/              # Проекты для тестирования
│   └── sample_projects/   # Примеры проектов
├── examples/              # Примеры использования
├── scripts/               # Скрипты сборки
└── tests/                 # Тесты
```

## Быстрый старт

### 1. Сборка для Desktop

```bash
cd simulator
chmod +x scripts/build_desktop.sh
./scripts/build_desktop.sh
```

### 2. Сборка для iOS

```bash
chmod +x scripts/build_ios.sh
./scripts/build_ios.sh
```

### 3. Сборка для Android

```bash
export ANDROID_NDK=/path/to/android-ndk
chmod +x scripts/build_android.sh
./scripts/build_android.sh
```

## Использование

### GUI Симулятор

```bash
cd build/desktop
./bin/SimulatorApp
```

### CLI Симулятор

```bash
# Показать справку
./scripts/simulator-cli --help

# Список проектов
./scripts/simulator-cli --list-projects

# Тестирование проекта
./scripts/simulator-cli test -p ./projects/basic_game

# Пакетное тестирование
./scripts/simulator-cli batch-test
```

### Makefile команды

```bash
# Сборка
make build

# Тестирование всех проектов
make test-all

# Unit тесты
make test-unit

# Тестирование конкретного проекта
make test PROJECT=./projects/basic_game
```

### Создание нового проекта

**GUI:**
1. Запустите симулятор
2. Выберите "Create New Project"
3. Введите имя проекта
4. Выберите шаблон
5. Нажмите "Create"

**CLI:**
```bash
./scripts/simulator-cli create -p ./projects/my_game
```

### Запуск симуляции

**GUI:**
1. Выберите проект из списка
2. Нажмите "Run Simulation"
3. Проект запустится в симуляторе

**CLI:**
```bash
./scripts/simulator-cli simulate -p ./projects/basic_game
```

### Тестирование

**GUI:**
1. Выберите проект
2. Нажмите "Run Tests"
3. Выберите типы тестов
4. Дождитесь завершения тестирования

**CLI:**
```bash
# Базовое тестирование
./scripts/simulator-cli test -p ./projects/basic_game

# Подробное тестирование
./scripts/simulator-cli test -p ./projects/basic_game -t unit,integration,performance -v

# Тестирование на всех платформах
./scripts/simulator-cli test -p ./projects/basic_game --platforms desktop,ios,android
```

## Примеры проектов

### Basic Game
Простая 2D игра с движущимся игроком и врагами.

**Особенности:**
- Управление WASD/стрелками
- Поддержка касаний
- Спавн врагов
- Камера следует за игроком

### Sprite Demo
Демонстрация рендеринга спрайтов и анимации.

**Особенности:**
- Анимированные спрайты
- Разные цвета и размеры
- Круговое движение
- Пульсация размера

### Input Test
Тестирование системы ввода.

**Особенности:**
- Тестирование клавиатуры
- Тестирование касаний
- Логирование ввода
- Визуальная обратная связь

## API симулятора

### ProjectSimulator

Главный класс симулятора.

```cpp
#include "ProjectSimulator/ProjectSimulator.h"

ProjectSimulator::ProjectSimulator simulator;
simulator.Initialize(1024, 768);
simulator.Run();
```

### ProjectManager

Управление проектами.

```cpp
auto project = projectManager->CreateProject("MyGame", "basic_game");
projectManager->SetActiveProject(project);
```

### SimulationEngine

Запуск симуляции.

```cpp
auto result = simulationEngine->RunProject(project);
if (result.success) {
    std::cout << "Simulation completed successfully" << std::endl;
}
```

## Конфигурация проектов

Проекты настраиваются через файл `project.conf`:

```ini
name=My Game
description=A cool game
version=1.0.0
author=Developer
main_file=src/main.cpp

asset=textures/player.png
asset=sounds/music.ogg

dependency=FastEngine
dependency=OpenGL

# Настройки
window_width=800
window_height=600
target_fps=60
```

## Тестирование

### Типы тестов

- **Unit Tests** - тестирование отдельных компонентов
- **Integration Tests** - тестирование взаимодействия систем
- **Performance Tests** - тестирование производительности
- **Compatibility Tests** - тестирование совместимости платформ

### Запуск тестов

```cpp
TestConfig config;
config.enabledTests = {TestType::Unit, TestType::Performance};
config.targetPlatforms = {"iOS", "Android"};

auto results = testRunner->RunTests(project, config);
```

## Разработка

### Добавление нового типа проекта

1. Создайте папку в `projects/sample_projects/`
2. Добавьте файл `project.conf`
3. Создайте исходный код в папке `src/`
4. Добавьте ассеты в папку `assets/`

### Расширение UI

```cpp
class CustomUIElement : public UIElement {
public:
    void Render() override {
        // Кастомная отрисовка
    }
};
```

## Требования

### Минимальные требования

**iOS:**
- iOS 12.0+
- iPhone 6s или новее
- 2GB RAM

**Android:**
- Android 5.0 (API 21)+
- ARM64 архитектура
- 2GB RAM

**Desktop:**
- macOS 10.14+, Linux, Windows 10
- OpenGL 3.3+
- 2GB RAM

### Рекомендуемые требования

**iOS:**
- iOS 14.0+
- iPhone 12 или новее
- 4GB RAM

**Android:**
- Android 8.0 (API 26)+
- ARM64 архитектура
- 4GB RAM

## Лицензия

MIT License - см. файл LICENSE для подробностей.

## Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте документацию FastEngine
2. Посмотрите примеры проектов
3. Создайте Issue в репозитории

## Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

---

**Project Simulator** - мощный инструмент для разработки и тестирования игр на FastEngine! 🚀
