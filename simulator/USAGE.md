# Руководство по использованию Project Simulator

## Быстрый старт

### 1. Сборка симулятора

```bash
# Для Desktop (macOS, Linux, Windows)
cd simulator
./scripts/build_desktop.sh

# Для iOS
./scripts/build_ios.sh

# Для Android
export ANDROID_NDK=/path/to/android-ndk
./scripts/build_android.sh
```

### 2. Запуск симулятора

```bash
cd build/desktop
./bin/SimulatorApp
```

### 3. Тестирование

```bash
cd simulator
./scripts/test_simulator.sh
```

## Основные функции

### Управление проектами

1. **Создание проекта:**
   - Выберите "Create New Project"
   - Введите имя проекта
   - Выберите шаблон (empty, basic_game, sprite_demo, input_test)
   - Нажмите "Create"

2. **Загрузка проекта:**
   - Проекты автоматически загружаются из папки `projects/`
   - Поддерживаются файлы `project.conf`

3. **Редактирование проекта:**
   - Выберите проект из списка
   - Нажмите "Edit Project"
   - Измените настройки в файле `project.conf`

### Симуляция проектов

1. **Запуск симуляции:**
   - Выберите проект
   - Нажмите "Run Simulation"
   - Проект запустится в изолированной среде

2. **Управление симуляцией:**
   - **Pause/Resume** - пауза/возобновление
   - **Stop** - остановка симуляции
   - **Restart** - перезапуск

3. **Мониторинг:**
   - FPS в реальном времени
   - Использование памяти
   - Логи выполнения

### Тестирование

1. **Запуск тестов:**
   - Выберите проект
   - Нажмите "Run Tests"
   - Выберите типы тестов:
     - Unit Tests
     - Integration Tests
     - Performance Tests
     - Compatibility Tests
     - Stress Tests

2. **Просмотр результатов:**
   - Результаты отображаются в реальном времени
   - Генерируется HTML отчет
   - Статистика по производительности

## Примеры проектов

### Basic Game
Простая 2D игра с движущимся игроком и врагами.

**Особенности:**
- Управление WASD/стрелками
- Поддержка касаний
- Спавн врагов
- Камера следует за игроком

**Запуск:**
1. Выберите "Basic Game" из списка проектов
2. Нажмите "Run Simulation"
3. Используйте WASD для движения игрока

### Sprite Demo
Демонстрация рендеринга спрайтов и анимации.

**Особенности:**
- Анимированные спрайты
- Разные цвета и размеры
- Круговое движение
- Пульсация размера

**Запуск:**
1. Выберите "Sprite Demo" из списка проектов
2. Нажмите "Run Simulation"
3. Наблюдайте за анимацией спрайтов

### Input Test
Тестирование системы ввода.

**Особенности:**
- Тестирование клавиатуры
- Тестирование касаний
- Логирование ввода
- Визуальная обратная связь

**Запуск:**
1. Выберите "Input Test" из списка проектов
2. Нажмите "Run Simulation"
3. Используйте клавиатуру и мышь для тестирования

## Конфигурация проектов

### Файл project.conf

```ini
# Основная информация
name=My Game
description=A cool game
version=1.0.0
author=Developer
main_file=src/main.cpp

# Ассеты
asset=textures/player.png
asset=sounds/music.ogg

# Зависимости
dependency=FastEngine
dependency=OpenGL

# Настройки
window_width=800
window_height=600
target_fps=60
fullscreen=false
```

### Структура проекта

```
MyProject/
├── project.conf          # Конфигурация проекта
├── src/
│   └── main.cpp         # Главный файл
├── assets/
│   ├── textures/        # Текстуры
│   └── sounds/          # Звуки
└── CMakeLists.txt       # Файл сборки
```

## API симулятора

### ProjectSimulator

```cpp
#include "ProjectSimulator/ProjectSimulator.h"

// Создание и инициализация
ProjectSimulator::ProjectSimulator simulator;
simulator.Initialize(1024, 768);

// Запуск главного цикла
simulator.Run();

// Получение компонентов
auto* projectManager = simulator.GetProjectManager();
auto* simulationEngine = simulator.GetSimulationEngine();
auto* uiManager = simulator.GetUIManager();
auto* testRunner = simulator.GetTestRunner();
```

### ProjectManager

```cpp
// Создание проекта
auto project = projectManager->CreateProject("MyGame", "basic_game");

// Загрузка проекта
auto project = projectManager->LoadProject("/path/to/project.conf");

// Сохранение проекта
projectManager->SaveProject(project);

// Получение списка проектов
auto projects = projectManager->GetProjects();

// Установка активного проекта
projectManager->SetActiveProject(project);
```

### SimulationEngine

```cpp
// Запуск симуляции
auto result = simulationEngine->RunProject(project);

// Управление симуляцией
simulationEngine->PauseSimulation();
simulationEngine->ResumeSimulation();
simulationEngine->StopSimulation();

// Получение информации
auto state = simulationEngine->GetState();
auto fps = simulationEngine->GetFPS();
auto logs = simulationEngine->GetLogs();
```

### TestRunner

```cpp
// Конфигурация тестирования
TestConfig config;
config.enabledTests = {TestType::Unit, TestType::Performance};
config.targetPlatforms = {"iOS", "Android"};
config.iterations = 10;

// Запуск тестов
auto results = testRunner->RunTests(project, config);

// Генерация отчета
testRunner->GenerateReport(results, "./test_reports/");

// Получение статистики
auto stats = testRunner->GetTestStatistics(results);
```

## Отладка и диагностика

### Логи

Симулятор ведет подробные логи:
- Инициализация компонентов
- Загрузка проектов
- Выполнение симуляции
- Результаты тестирования
- Ошибки и предупреждения

### Профилирование

- FPS в реальном времени
- Время выполнения кадра
- Использование памяти
- Время загрузки ассетов

### Ошибки

Типичные ошибки и их решения:

1. **"Project not found"**
   - Проверьте путь к проекту
   - Убедитесь, что файл `project.conf` существует

2. **"Failed to initialize FastEngine"**
   - Проверьте настройки окна
   - Убедитесь, что OpenGL поддерживается

3. **"Asset not found"**
   - Проверьте пути к ассетам
   - Убедитесь, что файлы существуют

## Расширение функциональности

### Добавление нового типа проекта

1. Создайте папку в `projects/sample_projects/`
2. Добавьте файл `project.conf`
3. Создайте исходный код в папке `src/`
4. Добавьте ассеты в папку `assets/`

### Создание кастомных тестов

```cpp
class CustomTest {
public:
    TestResult RunTest(std::shared_ptr<Project> project) {
        // Ваша логика тестирования
        return {testName, TestType::Unit, true, "Test passed", 1.0f, "Desktop", {}};
    }
};
```

### Интеграция с внешними инструментами

```cpp
// Callback для обновления UI
simulationEngine->SetUpdateCallback([](float deltaTime) {
    // Обновление UI
});

// Callback для обработки ошибок
simulationEngine->SetErrorCallback([](const std::string& error) {
    // Обработка ошибок
});
```

## Производительность

### Рекомендации

1. **Ограничение FPS:**
   ```cpp
   simulationEngine->SetLimits(60.0f, 512); // 60 FPS, 512 MB
   ```

2. **Оптимизация памяти:**
   - Используйте пулы объектов
   - Очищайте неиспользуемые ресурсы
   - Мониторьте использование памяти

3. **Многопоточность:**
   - Тесты выполняются в отдельных потоках
   - Симуляция может быть приостановлена для тестирования

## Поддержка

### Получение помощи

1. Проверьте логи симулятора
2. Посмотрите примеры проектов
3. Изучите документацию FastEngine
4. Создайте Issue в репозитории

### Сообщение об ошибках

При сообщении об ошибке укажите:
- Версию симулятора
- Операционную систему
- Логи выполнения
- Шаги для воспроизведения

---

**Project Simulator** - мощный инструмент для разработки и тестирования игр на FastEngine! 🚀

