# Руководство по использованию Advanced Game Editor

## Обзор

Advanced Game Editor - это полнофункциональный визуальный редактор для создания 2D игр на движке FastEngine. Редактор включает в себя все основные панели и инструменты, необходимые для профессиональной разработки игр.

## Установка и запуск

### Требования
- macOS 10.14+ (или Linux/Windows с соответствующими зависимостями)
- CMake 3.16+
- C++17 компилятор
- OpenGL 3.3+
- SDL2
- jsoncpp

### Сборка
```bash
cd /Users/igor/Desktop/GameAppsAi/FastEngine/advanced_editor
mkdir -p build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j4
```

### Запуск
```bash
./AdvancedGameEditor
```

## Интерфейс редактора

### 🎬 Scene View (Панель сцены)
**Центральная панель редактора**

- **Панорамирование**: Перетаскивание мышью для перемещения камеры
- **Масштабирование**: Колесико мыши для увеличения/уменьшения
- **Сетка**: Визуальная сетка для точного размещения объектов
- **Перетаскивание объектов**: Левой кнопкой мыши для выбора и перемещения
- **Создание объектов**: Правой кнопкой мыши для создания новых объектов
- **Привязка к сетке**: Автоматическое выравнивание объектов по сетке

### ⚙️ Inspector Panel (Панель свойств)
**Отображение и редактирование свойств выбранного объекта**

- **Transform**: Позиция (X, Y), масштаб (Scale X, Y), поворот (Rotation)
- **Sprite Renderer**: Тип объекта, размер, цвет (RGB)
- **Компоненты**: Возможность добавления/удаления компонентов

### 🗂 Hierarchy Panel (Панель иерархии)
**Список всех объектов на сцене**

- **Древовидная структура**: Отображение всех объектов сцены
- **Выбор объектов**: Клик для выбора объекта
- **Выделение**: Выбранный объект подсвечивается желтым цветом

### 📦 Assets Panel (Панель ресурсов)
**Управление ресурсами проекта**

- **Sprites**: Спрайты (Player, Enemy, Ground)
- **Graphics**: Графические элементы (Background, UI Elements)
- **Drag & Drop**: Перетаскивание ресурсов на сцену

### ⌨️ Console Panel (Панель консоли)
**Логи и сообщения системы**

- **Статус операций**: Сообщения о создании, выборе, сохранении объектов
- **Ошибки**: Отображение ошибок и предупреждений
- **Отладочная информация**: Полезная информация для разработчика

### 🎮 Game Preview Panel (Панель предпросмотра)
**Предпросмотр игры в редакторе**

- **Play Mode**: Переключение между режимом редактирования и воспроизведения
- **Статус**: Отображение текущего состояния (PLAYING/PAUSED)

## Управление

### Основные клавиши

| Клавиша | Действие |
|---------|----------|
| **ESC** | Выход из редактора |
| **Space** | Переключение отображения сетки |
| **G** | Переключение привязки к сетке |
| **S** | Сохранение проекта |
| **L** | Загрузка проекта |
| **N** | Создание нового проекта |
| **P** | Переключение режима воспроизведения |

### Управление панелями

| Клавиша | Действие |
|---------|----------|
| **F1** | Переключение панели Inspector |
| **F2** | Переключение панели Hierarchy |
| **F3** | Переключение панели Assets |
| **F4** | Переключение панели Console |
| **F5** | Переключение панели Game Preview |

### Управление камерой

| Клавиша | Действие |
|---------|----------|
| **W** | Движение камеры вверх |
| **S** | Движение камеры вниз |
| **A** | Движение камеры влево |
| **D** | Движение камеры вправо |
| **Колесико мыши** | Масштабирование |

### Управление объектами

| Клавиша | Действие |
|---------|----------|
| **Стрелки** | Движение выбранного объекта |
| **Левая кнопка мыши** | Выбор объекта |
| **Правая кнопка мыши** | Создание нового объекта |
| **Перетаскивание** | Перемещение объекта |

## Работа с проектами

### Создание нового проекта
1. Нажмите клавишу **N**
2. Все существующие объекты будут удалены
3. Создается пустая сцена с тестовыми объектами

### Сохранение проекта
1. Нажмите клавишу **S**
2. Проект сохраняется в файл `advanced_game.json`
3. В консоли появится сообщение "Project saved to advanced_game.json"

### Загрузка проекта
1. Нажмите клавишу **L**
2. Загружается проект из файла `advanced_game.json`
3. Если файл не найден, создается пустая сцена

## Работа с объектами

### Создание объектов
1. Используйте правую кнопку мыши в области сцены
2. Объект создается в позиции клика
3. Объект автоматически выбирается после создания
4. Имя объекта генерируется автоматически (Object_1, Object_2, и т.д.)

### Выбор объектов
1. Кликните левой кнопкой мыши на объект
2. Выбранный объект подсвечивается желтым цветом
3. В панели Inspector отображаются свойства объекта
4. В панели Hierarchy выбранный объект выделяется

### Перемещение объектов
1. Выберите объект
2. Перетащите его мышью или используйте стрелки
3. Если включена привязка к сетке, объект будет привязываться к узлам сетки

### Редактирование свойств
1. Выберите объект
2. В панели Inspector измените нужные свойства:
   - **Position**: Позиция объекта (X, Y)
   - **Scale**: Масштаб объекта (Scale X, Y)
   - **Rotation**: Поворот объекта в градусах
   - **Size**: Размер объекта
   - **Color**: Цвет объекта (RGB)

## Формат проекта

Проекты сохраняются в формате JSON:

```json
{
  "name": "My Advanced Game",
  "version": "1.0.0",
  "entities": [
    {
      "name": "Player",
      "type": "Sprite",
      "x": 100.0,
      "y": 200.0,
      "scaleX": 1.0,
      "scaleY": 1.0,
      "rotation": 0.0,
      "size": 32.0,
      "r": 1.0,
      "g": 1.0,
      "b": 0.0,
      "visible": true
    }
  ]
}
```

### Структура файла проекта

- `name` - Название проекта
- `version` - Версия проекта
- `entities` - Массив объектов в сцене
  - `name` - Имя объекта
  - `type` - Тип объекта (Sprite, Camera, UI и т.д.)
  - `x`, `y` - Позиция объекта
  - `scaleX`, `scaleY` - Масштаб объекта
  - `rotation` - Поворот объекта в градусах
  - `size` - Размер объекта
  - `r`, `g`, `b` - Цвет объекта (RGB)
  - `visible` - Видимость объекта

## Настройки

### Размер сетки
По умолчанию размер сетки составляет 32 пикселя. Это значение можно изменить в коде редактора.

### Скорость движения
- Камера: 300 пикселей в секунду
- Объекты: 200 пикселей в секунду

### Масштабирование
- Минимальный масштаб: 0.1x
- Максимальный масштаб: 5.0x
- Шаг масштабирования: 1.1x

## Примеры использования

### Создание простой сцены
1. Запустите редактор
2. Нажмите **N** для создания нового проекта
3. Используйте правую кнопку мыши для создания объектов
4. Перемещайте объекты левой кнопкой мыши
5. Нажмите **S** для сохранения

### Редактирование существующей сцены
1. Запустите редактор
2. Нажмите **L** для загрузки проекта
3. Выберите объекты кликом
4. Измените их свойства в панели Inspector
5. Нажмите **S** для сохранения изменений

### Настройка интерфейса
1. Используйте клавиши F1-F5 для переключения панелей
2. Настройте интерфейс под свои потребности
3. Все настройки сохраняются в сессии

## Расширенные возможности

### Режим воспроизведения
- Нажмите **P** для переключения в режим воспроизведения
- В этом режиме объекты ведут себя как в игре
- Нажмите **P** снова для возврата в режим редактирования

### Масштабирование сцены
- Используйте колесико мыши для масштабирования
- Масштабирование работает только в области сцены
- Камера автоматически адаптируется к новому масштабу

### Перетаскивание объектов
- Выберите объект и перетащите его мышью
- Объект будет следовать за курсором мыши
- Привязка к сетке работает в реальном времени

## Ограничения

- Редактор работает только в консольном режиме (без графического интерфейса)
- Поддерживается только один тип объектов (Sprite)
- Нет возможности удаления объектов
- Нет возможности изменения текстуры объектов
- Нет встроенного редактора анимации

## Разработка

### Структура кода
- `AdvancedGameEditor` - Основной класс редактора
- `Entity` - Структура объекта сцены
- `Panel` - Структура панели интерфейса
- `Initialize()` - Инициализация редактора
- `Update()` - Обновление логики
- `Render()` - Отрисовка интерфейса
- `HandleEvent()` - Обработка событий

### Добавление новых функций
1. Добавьте новые клавиши в `HandleKeyDown()`
2. Реализуйте логику в соответствующих методах
3. Обновите документацию

## Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что движок FastEngine собран
3. Проверьте права доступа к файлам проекта
4. Посмотрите сообщения в панели Console

## Заключение

Advanced Game Editor предоставляет полнофункциональный интерфейс для создания 2D игр. Хотя он имеет некоторые ограничения, он служит отличной основой для дальнейшего развития и может быть расширен дополнительными функциями по мере необходимости.

Редактор включает в себя все основные панели, описанные в вашем списке виджетов:
- ✅ Scene View (Панель сцены)
- ✅ Inspector Panel (Панель свойств)
- ✅ Hierarchy Panel (Панель иерархии)
- ✅ Assets Panel (Панель ресурсов)
- ✅ Console Panel (Панель консоли)
- ✅ Game Preview Panel (Панель предпросмотра)

Это создает прочную основу для дальнейшего развития редактора с дополнительными функциями, такими как редактор анимации, встроенный редактор скриптов, и более продвинутые инструменты редактирования.

