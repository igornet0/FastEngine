# FastEngine Qt Editor

Современный редактор для FastEngine с поддержкой 2D и 3D сцен, построенный на Qt6.

## Возможности

### 🎮 Редактирование сцен
- **2D режим**: Редактирование 2D сцен с поддержкой спрайтов, анимаций и UI элементов
- **3D режим**: Редактирование 3D сцен с поддержкой моделей, освещения и материалов
- **Переключение режимов**: Быстрое переключение между 2D и 3D режимами
- **Управление камерой**: Интуитивное управление камерой в обоих режимах

### 🛠️ Панели редактора
- **Scene View**: Основная панель для редактирования сцены
- **Inspector**: Редактирование свойств выбранных объектов
- **Hierarchy**: Управление иерархией объектов сцены
- **Assets**: Управление ресурсами проекта с предпросмотром
- **Console**: Вывод сообщений, ошибок и предупреждений

### 📁 Управление проектами
- **Создание проектов**: Создание новых проектов FastEngine
- **Загрузка проектов**: Загрузка существующих проектов
- **Сохранение проектов**: Автосохранение и ручное сохранение
- **Экспорт проектов**: Экспорт для различных платформ

### 🎨 Редактирование объектов
- **Создание объектов**: Создание объектов в сцене
- **Перемещение объектов**: Перетаскивание объектов мышью
- **Редактирование свойств**: Изменение свойств через Inspector
- **Компоненты**: Добавление и удаление компонентов
- **Анимации**: Редактирование анимаций объектов

### 🔧 Интеграция с FastEngine
- **Полная интеграция**: Использование всех возможностей FastEngine
- **ECS архитектура**: Поддержка Entity-Component-System
- **Рендеринг**: Использование FastEngine рендерера
- **Физика**: Интеграция с физической системой
- **Аудио**: Управление аудио ресурсами

## Требования

### Системные требования
- **ОС**: Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
- **RAM**: 8 GB (рекомендуется 16 GB)
- **GPU**: OpenGL 3.3+ или Vulkan 1.0+
- **Место на диске**: 2 GB свободного места

### Зависимости
- **Qt6**: 6.0 или выше
- **CMake**: 3.16 или выше
- **C++17**: Компилятор с поддержкой C++17
- **FastEngine**: Встроенная библиотека

## Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-repo/FastEngine.git
cd FastEngine/qt_editor
```

### 2. Установка зависимостей

#### Ubuntu/Debian
```bash
sudo apt update
sudo apt install qt6-base-dev qt6-tools-dev cmake build-essential
```

#### macOS
```bash
brew install qt6 cmake
```

#### Windows
- Установите Qt6 через официальный установщик
- Установите CMake
- Установите Visual Studio 2019+ или MinGW

### 3. Сборка
```bash
mkdir build
cd build
cmake ..
make -j4
```

### 4. Запуск
```bash
./FastEngineQtEditor
```

## Использование

### Создание нового проекта
1. Запустите редактор
2. Выберите `File > New Project`
3. Введите имя проекта
4. Выберите папку для проекта
5. Нажмите `Create`

### Редактирование 2D сцены
1. Переключитесь в 2D режим (кнопка `2D Mode`)
2. Используйте правую кнопку мыши для создания объектов
3. Перетаскивайте объекты левой кнопкой мыши
4. Редактируйте свойства в панели Inspector

### Редактирование 3D сцены
1. Переключитесь в 3D режим (кнопка `3D Mode`)
2. Используйте правую кнопку мыши для создания объектов
3. Управляйте камерой:
   - ЛКМ + перетаскивание: поворот камеры
   - ПКМ + перетаскивание: перемещение камеры
   - Колесо мыши: масштабирование
4. Редактируйте свойства в панели Inspector

### Управление ресурсами
1. Откройте панель Assets
2. Импортируйте ресурсы через `Import Asset`
3. Перетаскивайте ресурсы в сцену
4. Управляйте ресурсами через контекстное меню

## Горячие клавиши

### Общие
- `Ctrl+N`: Новый проект
- `Ctrl+O`: Открыть проект
- `Ctrl+S`: Сохранить проект
- `Ctrl+Shift+S`: Сохранить как
- `Ctrl+Z`: Отменить
- `Ctrl+Y`: Повторить
- `Delete`: Удалить выбранные объекты
- `F5`: Воспроизвести сцену
- `F6`: Пауза сцены
- `F7`: Остановить сцену

### Редактирование
- `G`: Переключить сетку
- `S`: Переключить привязку к сетке
- `0`: Сбросить камеру
- `WASD`: Управление камерой (3D режим)
- `Стрелки`: Управление выбранным объектом

### Панели
- `F1`: Переключить Inspector
- `F2`: Переключить Hierarchy
- `F3`: Переключить Assets
- `F4`: Переключить Console
- `F5`: Переключить Game Preview

## Архитектура

### Основные компоненты
- **MainWindow**: Главное окно редактора
- **SceneView2D**: 2D панель сцены
- **SceneView3D**: 3D панель сцены
- **InspectorPanel**: Панель свойств
- **HierarchyPanel**: Панель иерархии
- **AssetsPanel**: Панель ресурсов
- **ConsolePanel**: Панель консоли
- **ProjectManager**: Менеджер проектов
- **FastEngineIntegration**: Интеграция с FastEngine

### Структура проекта
```
qt_editor/
├── CMakeLists.txt          # Конфигурация сборки
├── main.cpp                # Точка входа
├── MainWindow.h/cpp        # Главное окно
├── SceneView2D.h/cpp       # 2D панель сцены
├── SceneView3D.h/cpp       # 3D панель сцены
├── InspectorPanel.h/cpp    # Панель свойств
├── HierarchyPanel.h/cpp    # Панель иерархии
├── AssetsPanel.h/cpp       # Панель ресурсов
├── ConsolePanel.h/cpp      # Панель консоли
├── ProjectManager.h/cpp    # Менеджер проектов
├── FastEngineIntegration.h/cpp # Интеграция с FastEngine
└── README.md               # Документация
```

## Разработка

### Добавление новых компонентов
1. Создайте класс компонента в FastEngine
2. Добавьте поддержку в FastEngineIntegration
3. Создайте виджет редактирования в InspectorPanel
4. Добавьте иконку в HierarchyPanel

### Добавление новых типов ресурсов
1. Добавьте поддержку в ResourceManager
2. Создайте генератор превью в AssetsPanel
3. Добавьте обработку в FastEngineIntegration

### Кастомизация интерфейса
1. Измените стили в main.cpp
2. Добавьте новые панели в MainWindow
3. Создайте новые действия в меню

## Устранение неполадок

### Проблемы с Qt6
- Убедитесь, что Qt6 установлен корректно
- Проверьте переменные окружения QT_DIR
- Пересоберите проект после изменения Qt

### Проблемы с OpenGL
- Обновите драйверы видеокарты
- Проверьте поддержку OpenGL 3.3+
- Используйте программный рендеринг при необходимости

### Проблемы с CMake
- Обновите CMake до версии 3.16+
- Очистите кэш CMake
- Проверьте пути к зависимостям

## Лицензия

Этот проект лицензирован под MIT License. См. файл LICENSE для подробностей.

## Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте документацию
2. Поищите в Issues на GitHub
3. Создайте новый Issue с подробным описанием проблемы

## Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:
1. Форкните репозиторий
2. Создайте ветку для ваших изменений
3. Сделайте коммит с описанием изменений
4. Создайте Pull Request

## Благодарности

- Qt Team за отличный фреймворк
- FastEngine Team за игровой движок
- Сообщество за обратную связь и предложения



